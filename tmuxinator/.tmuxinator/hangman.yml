# ~/.tmuxinator/hangman.yml

name: hangman
root: ~/code/hangman
windows:
  - hangman:
      root: ~/code/hangman
      layout: main-vertical
      panes:
        - nvim
        - iex -S mix
        - mix test
  - credo:
      layout: main-vertical
      panes:
        - mix credo
  - git:
      layout: main-vertical
      panes:
        - tig log
